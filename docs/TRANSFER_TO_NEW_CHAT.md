# SmartLife — перенос в новый чат

## Проект
- Название: SmartLife
- Репозиторий: `https://github.com/messireL/SmartLife`
- Путь на сервере: `/opt/SmartLife`
- Стек: Python + FastAPI + PostgreSQL + Docker Compose
- Сервер работает в LAN, публикация обычно на `192.168.x.x:13443`
- Часовой пояс проекта: `Europe/Moscow`

## Как работаем
1. Сначала делается релиз/архив.
2. Commit message даётся отдельно.
3. Пользователь пушит через GitHub Desktop.
4. Сервер обновляется **только через git**: `git pull --ff-only`, затем `./scripts/manage.sh up`. Скрипт сам делает rebuild по умолчанию.
5. Не накатывать архив поверх git-репозитория как основной сценарий.

## Что уже есть
- Tuya Cloud интеграция
- Фоновая синхронизация
- Графики и дашборд
- Комнаты и локальные имена устройств
- Бэкапы БД
- Управление устройствами через `switch`, `switch_1`, а также поддержка бойлера через `mode` и `temp_set`
- Cloud-настройки хранятся в PostgreSQL (`app_settings`)
- Тариф электроэнергии поддерживает единый, двухзонный и трёхзонный режимы по времени суток
- В разделе Настройки есть веб-форма изменения cloud-настроек
- `docs/SERVER_COMMANDS.md` содержит актуальные команды для сервера
- `./scripts/manage.sh runtime-info` показывает диагностику runtime, тарифного месяца и схемы БД

## Важные правила
- В каждый релиз класть и обновлять этот файл и `docs/SERVER_COMMANDS.md`.
- В обычных командах `health` и `url` не чистить экран.
- `./scripts/manage.sh up` теперь по умолчанию пересобирает контейнеры; для редкого запуска без rebuild есть `./scripts/manage.sh up --no-build`.
- Очистку stopped-контейнеров делать только для mutating-команд или отдельно через `cleanup-docker`.
- demo-устройства можно чистить, обычные устройства не удалять из UI.

## Ближайшие идеи
- сценарии / расписания
- HTTPS для LAN через reverse proxy (Caddy/Nginx) и локальный сертификат
- дальнейшее улучшение управления устройствами beyond текущих `switch`, `switch_1`, `mode`, `temp_set`


Примечание: manage.sh в новых релизах принудительно убирает COMPOSE_IGNORE_ORPHANS из окружения перед вызовом docker compose, чтобы не конфликтовать с --remove-orphans.


## Обновление v0.8.0
- Добавлен тариф электроэнергии с режимами: единый, двухзонный, трёхзонный.
- Стоимость считается по времени снапшотов и раскладывается по зонам.
- В Настройках можно задать ставки и временные окна для зон.


## Обновление v0.9.2
- Тарифы переключаются вкладками в Настройках и сохраняются с 1 числа месяца.
- Если сегодня не первое число, изменения тарифа автоматически планируются на следующий месяц.
- История тарифов хранится в PostgreSQL и используется в расчётах стоимости и kWh по зонам за соответствующий месяц.


## Обновление v0.9.3
- Добавлена диагностика runtime / БД: `/health`, `/api/runtime/diagnostics`, раздел в Настройках и `./scripts/manage.sh runtime-info`.
- Старт приложения теперь делает явный self-check и падает с понятной ошибкой, если на старой БД не хватает обязательных таблиц или столбцов.
- В Настройках видно активный тарифный месяц, дату следующего вступления плана и предупреждения по конфигурации провайдера.



## Обновление v0.9.6
- Добавлена базовая поддержка бойлеров / температурных устройств Tuya Cloud: текущая температура, целевая температура, режим, fault и команды управления в карточке устройства.
- Главная, Потребление и карточка устройства теперь показывают не только общий итог, но и разбивку по активному тарифу (1 / 2 / 3 зоны) с суммой по каждой зоне.
- Интерфейс сделан спокойнее: облегчён шрифт, уменьшена жирность чисел и поправлена укладка длинных значений в карточках и сайдбаре.


## HTTP / HTTPS

Сейчас SmartLife публикуется в LAN по HTTP, потому что стек поднимает приложение напрямую на локальном IP и порту без отдельного reverse proxy и TLS-сертификата. Для HTTPS нужен отдельный слой терминатора TLS (например, Caddy или Nginx) и локальный сертификат/доверенный CA.


## Обновление v0.10.1

- Проведён UI-polish релиз: типографика, карточки, сайдбар, кнопки и плашки статусов сделаны легче и аккуратнее.
- Hero-тексты на Главной, в Потреблении и в карточке устройства стали спокойнее и более утилитарными.
- Для следующего UX-релиза можно отдельно делать HTTPS в LAN и ещё один проход по графикам/таблицам.


## Обновление v0.10.2

- добавлена поддержка сетевых фильтров / удлинителей Tuya с несколькими розетками и USB-блоком;
- в карточке устройства появились каналы питания и отдельное управление по каждому каналу;
- список устройств стал лучше показывать технический профиль и число каналов.


## Обновление v0.10.3

- на вкладке «Управление» для многоканальных устройств рядом с каждым каналом теперь видно его текущее состояние;
- кнопки включения/выключения для канала теперь приглушаются, если канал уже находится в нужном состоянии;
- это особенно полезно для удлинителей с несколькими розетками и USB-блоком.

## Обновление v0.10.6

- после обновления запусти `./scripts/manage.sh sync`, чтобы SmartLife подтянул свежие `function/status definitions` из Tuya;
- открой карточку удлинителя → вкладка `Управление`: там должны появиться countdown по каналам, поведение после подачи питания, режим индикатора и детский замок;
- если какое-то enum-поле пока пустое, повторный `sync` обычно подтягивает варианты без ручных правок.

## Что добавлено в v0.10.6

- поддержка расширенных команд Tuya для удлинителей: `countdown_N`, `relay_status`, `light_mode`, `child_lock`;
- формы управления этими параметрами прямо в SmartLife;
- использование definitions из Tuya для валидации значений и отображения допустимых вариантов.
