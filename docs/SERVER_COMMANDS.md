# SmartLife — команды для сервера

## Обновление сервера через git
```bash
cd /opt/SmartLife
git reset --hard HEAD
git pull --ff-only
chmod +x scripts/manage.sh
./scripts/manage.sh up
./scripts/manage.sh health
./scripts/manage.sh runtime-info
./scripts/manage.sh url
```

`./scripts/manage.sh up` по умолчанию делает rebuild контейнеров. Если когда-нибудь понадобится запуск без rebuild, используй `./scripts/manage.sh up --no-build`.

## Настройка облака Tuya
```bash
cd /opt/SmartLife
./scripts/manage.sh configure-tuya
./scripts/manage.sh up
./scripts/manage.sh health
./scripts/manage.sh runtime-info
./scripts/manage.sh url
```

## Ручная очистка demo-устройств
```bash
cd /opt/SmartLife
./scripts/manage.sh cleanup-demo
```

## Бэкапы
```bash
cd /opt/SmartLife
./scripts/manage.sh backup-db
./scripts/manage.sh backup-list
./scripts/manage.sh restore-db backups/db/<file>.dump
```

## Часовой пояс, диагностика и пересчёт энергии
```bash
cd /opt/SmartLife
./scripts/manage.sh configure-timezone Europe/Moscow
./scripts/manage.sh runtime-info
./scripts/manage.sh rebuild-energy
```

## Ручная docker-cleanup
```bash
cd /opt/SmartLife
./scripts/manage.sh cleanup-docker
```


Примечание: manage.sh в новых релизах принудительно убирает COMPOSE_IGNORE_ORPHANS из окружения перед вызовом docker compose, чтобы не конфликтовать с --remove-orphans.


## Обновление v0.8.0
- Добавлен тариф электроэнергии с режимами: единый, двухзонный, трёхзонный.
- Стоимость считается по времени снапшотов и доступна с разбивкой по зонам.
- Настраивается через раздел Настройки в веб-интерфейсе.


## Обновление v0.9.2
- После обновления зайди в Настройки и проверь активный тариф и, при необходимости, план на следующий месяц.
- Разбивка kWh по зонам работает даже при нулевых ценах.


## Обновление v0.9.3
- После обновления можно быстро проверить runtime через `./scripts/manage.sh runtime-info`.
- `./scripts/manage.sh health` теперь показывает расширенную картину по тарифу, схеме БД и предупреждениям.


## Обновление v0.9.6
- После обновления запусти обычную синхронизацию (`./scripts/manage.sh up` уже её не делает принудительно, но фон подхватит данные сам).
- Если бойлер уже виден в Tuya Cloud, после ближайшей синхронизации в карточке устройства появятся температура, режим и команды управления.
- Для свежей проверки можно открыть `/devices`, найти бойлер и убедиться, что у него появились `temp_current`, `temp_set` и/или `mode` в UI.
- На Главной и в Потреблении проверь карточки Сегодня / Месяц: теперь там должны быть kWh, сумма в валюте и разбивка по активным тарифным зонам.


## HTTP / HTTPS

Сейчас SmartLife публикуется в LAN по HTTP, потому что стек поднимает приложение напрямую на локальном IP и порту без отдельного reverse proxy и TLS-сертификата. Для HTTPS нужен отдельный слой терминатора TLS (например, Caddy или Nginx) и локальный сертификат/доверенный CA.


## Обновление v0.10.1

- После обновления проверь Главную, Потребление и карточку бойлера: шрифт и карточки должны выглядеть легче и чище.
- В сайдбаре LAN URL теперь должен укладываться аккуратнее, без некрасивого разрыва на крупном шрифте.
- Если захочется следующий шаг, отдельным релизом можно вынести LAN-доступ на HTTPS через Caddy/Nginx и локальный сертификат.


## Обновление v0.10.3

- добавлена поддержка сетевых фильтров / удлинителей Tuya с несколькими розетками и USB-блоком;
- в карточке устройства появились каналы питания и отдельное управление по каждому каналу;
- список устройств стал лучше показывать технический профиль и число каналов.

## Обновление v0.10.6

- после обновления запусти `./scripts/manage.sh sync`, чтобы SmartLife подтянул свежие `function/status definitions` из Tuya;
- открой карточку удлинителя → вкладка `Управление`: там должны появиться countdown по каналам, поведение после подачи питания, режим индикатора и детский замок;
- если какое-то enum-поле пока пустое, повторный `sync` обычно подтягивает варианты без ручных правок.

## Что добавлено в v0.10.6

- поддержка расширенных команд Tuya для удлинителей: `countdown_N`, `relay_status`, `light_mode`, `child_lock`;
- формы управления этими параметрами прямо в SmartLife;
- использование definitions из Tuya для валидации значений и отображения допустимых вариантов.


## Обновление v0.10.7

- добавлено отображение тока: на Главной, в Потреблении, в списке устройств и в карточке устройства;
- для удлинителей и сетевых фильтров ток теперь отдельно виден в блоке суммарной энергии устройства;
- без поканальных DP-кодов Tuya ток по каналам остаётся только там, где железка реально его отдаёт.


## Обновление v0.10.8

- после обновления проверь вкладку `Локально` у удлинителя: у каждого канала должны появиться поля имени, роли и иконки;
- открой раздел `Плашки`: существующие плашки теперь можно редактировать прямо в таблице;
- отдельный `sync` для этого релиза не обязателен, но не повредит, если хочешь сразу подтянуть свежие статусы из Tuya.


## Обновление v0.10.9

- после обновления открой карточку удлинителя → `Управление`: там появятся быстрые групповые действия;
- можно одним действием включать/выключать все каналы, все розетки, USB-блок или целую роль;
- вкладка `Обзор` теперь группирует каналы по ролям и типам, чтобы карточка устройства читалась спокойнее.


## Обновление v0.11.0

- после обновления открой раздел `Сценарии` и создай хотя бы одно правило для теста;
- для проверки без ожидания времени можно нажать `Запустить сейчас`;
- сценарии исполняются фоновым циклом приложения, поэтому отдельный cron на сервере не нужен.


## Обновление v0.11.1

- добавлен мост к Tuya-сценам: раздел `Сценарии` теперь умеет показывать штатные Tap-to-Run сцены и Tuya-автоматизации по списку `home_id`;
- Tuya-сцену можно запускать вручную из SmartLife, а Tuya-автоматизацию — включать и выключать;
- локальный сценарий SmartLife теперь может в качестве цели запускать Tuya-сцену по расписанию;
- в `Настройках` появился отдельный блок для ввода `home_id`, по которым SmartLife подтягивает сцены и автоматизации.


## Обновление v0.11.5

- раздел `Сценарии` получил поиск и фильтры по активности, типу цели и статусу журналов;
- локальные сценарии теперь можно ставить на паузу, включать обратно и дублировать без ручного пересоздания;
- журнал запусков показывает не только успех/ошибку, но и тип запуска (`ручной`, `по расписанию`) и более понятный итог.


## Обновление v0.11.6

- после обновления открой `Сценарии`: фильтры теперь лежат в одной верхней панели и зависят от выбранной вкладки;
- во вкладке `Tuya-мост` можно фильтровать отдельно `Tap-to-Run` и `Automation`, а также активные/выключенные элементы;
- если выдача пуста, интерфейс теперь должен явно объяснять, что это пустой дом или слишком строгий фильтр.


## Обновление v0.11.7

- после обновления открой `Сценарии` → `Локальные`: в списке целей должны появиться группы по комнате, плашке и роли канала;
- для проверки создай, например, сценарий “Выключить все зарядки” или “Выключить кухня” и нажми `Запустить сейчас`;
- в журнале должен появиться итог с количеством успешно обработанных каналов и возможными ошибками по отдельным устройствам.


## Обновление v0.11.8

- после обновления открой `Сценарии` → `Локальные` и выбери любую группу: под селектом должен появиться живой предпросмотр состава;
- в карточке существующего группового правила теперь должен быть виден текущий состав группы и предупреждение, если она опустела после sync или переименований;
- в `Журнале` у групповых запусков теперь должны быть и краткий итог, и отдельная строка с ошибками, если кто-то из участников закапризничал.

## Обновление v0.11.9

- после обновления открой `Сценарии` → `Локальные` и выбери `Tap-to-Run`: поле действия должно автоматически заблокироваться;
- выбери пустую группу, если такая есть: кнопка создания/сохранения сценария должна стать неактивной, а в карточке существующего правила появится плашка `пустая цель`;
- во вкладке `Журнал` теперь должна быть отдельная колонка `Цель`, чтобы сразу видеть, что запускалось.
